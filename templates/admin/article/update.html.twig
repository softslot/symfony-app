{% extends 'base.html.twig' %}

{% block body %}
  <h1>edit article</h1>

  {{ form_start(form) }}
  {{ form_widget(form) }}
  <button type="submit" class="btn btn-primary">Update!</button>
  {{ form_end(form) }}
{% endblock %}
